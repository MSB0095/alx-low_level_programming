#include<stdio.h>

/**
 * main - Entry point (only prints 'with proper grammar, but the outcome is a piece of art,')
 * Return: Always 0 (Success)
 */

int main (void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
