#include <stdio.h>

/**
 * main - prints the program's path 
 * @argc: arguments count
 * @argv: arguments vector
 * 
 * Return: 0
*/
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
