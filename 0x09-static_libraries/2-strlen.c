#include "main.h"
#include <string.h>

/**
 * _strlen - a function that returns the length of a string.
 * @s: the string pointer
 * Return: the lenght of the string pointed by *s
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
